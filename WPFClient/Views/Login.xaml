<UserControl x:Class="WPFClient.Views.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFClient.Views"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:pwdEx="clr-namespace:WPFClient.Extensions"
             Height="350" Width="800">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="Width" Value="800"/>
            <Setter Property="Height" Value="350"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <md:Snackbar x:Name="RegisterLoginBar" Grid.ColumnSpan="2" MessageQueue="{md:MessageQueue}"></md:Snackbar>
        <Image Source="/images/computer_azarashi.png" />
        <md:Transitioner Grid.Column="1" SelectedIndex="{Binding SelectedIndex, FallbackValue=0}">
            <md:TransitionerSlide Margin="0,0,30,0">
                <DockPanel Margin="15" VerticalAlignment="Center">
                    <TextBlock Text="ログイン" FontSize="24" FontWeight="Bold" Margin="0,10" DockPanel.Dock="Top" />
                    <TextBox md:HintAssist.Hint="ユーザー名" Margin="0,10"  DockPanel.Dock="Top" Text="{Binding Account}"  />
                    <PasswordBox md:HintAssist.Hint="パスポート" Margin="0,10"  DockPanel.Dock="Top" 
                                 pwdEx:PasswordBoxExtend.Pwd="{Binding Pwd, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Behaviors>
                            <pwdEx:PasswordBoxBehavior></pwdEx:PasswordBoxBehavior>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <Button Content="ログイン" Margin="0,10" Height="35" DockPanel.Dock="Top" Command="{Binding LoginCmm}" />
                    <DockPanel Margin="0,5" LastChildFill="False">
                        <TextBlock Text="新規登録">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding SwitchInfoCmm}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                        <TextBlock Text="パスワードをお忘れですか？" DockPanel.Dock="Right">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                    </DockPanel>
                </DockPanel>
            </md:TransitionerSlide>
            <md:TransitionerSlide Margin="0,0,30,0">
                <DockPanel Margin="15" VerticalAlignment="Center">
                    <TextBlock Text="新規登録" FontSize="24" FontWeight="Bold" Margin="0,10" DockPanel.Dock="Top" />
                    <TextBox md:HintAssist.Hint="ニックネーム" Margin="0,10"  DockPanel.Dock="Top"  Text="{Binding AccountInfo.Nickname}" />
                    <TextBox md:HintAssist.Hint="ユーザー名" Margin="0,10"  DockPanel.Dock="Top"  Text="{Binding AccountInfo.Account}" />
                    <PasswordBox md:HintAssist.Hint="パスポート" Margin="0,10"  DockPanel.Dock="Top" 
             pwdEx:PasswordBoxExtend.Pwd="{Binding AccountInfo.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Behaviors>
                            <pwdEx:PasswordBoxBehavior></pwdEx:PasswordBoxBehavior>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <PasswordBox md:HintAssist.Hint="パスポートを確認" Margin="0,10"  DockPanel.Dock="Top" 
             pwdEx:PasswordBoxExtend.Pwd="{Binding Pwd, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Behaviors>
                            <pwdEx:PasswordBoxBehavior></pwdEx:PasswordBoxBehavior>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <Button Content="登録"  Height="35" DockPanel.Dock="Top" Command="{Binding RegisterCmm}" />
                    <Button Content="ログインに戻り" Margin="0,10" Height="35" DockPanel.Dock="Top" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding SwitchInfoCmm}"  />
                </DockPanel>
            </md:TransitionerSlide>
        </md:Transitioner>
    </Grid>
</UserControl>
